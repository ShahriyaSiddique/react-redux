import React from "react";

function HomeContainer() {
  return (
    <div>
      <h1>Number of cakes</h1>
      <button onClick={() => alert("hellos")}>Click</button>
    </div>
  );
}

export default HomeContainer;
